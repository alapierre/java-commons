name: CI Verify
on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  verify:
    uses: ./.github/workflows/reusable/_verify.yml
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}